## Markdown to PDF

When converting a Markdown resume to PDF, use:

`npx --yes md-to-pdf resume.md`

## PDF to Markdown

When converting a PDF resume to Markdown, use Markdownify MCP:

`mcp__markdownify__pdf-to-markdown`

Warning: output quality can vary, useful for extracting raw text without structure
